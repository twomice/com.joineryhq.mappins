(function(angular, $, _) {

  // Controller for the "Extension Overlay" dialog content.
  angular.module('mappins').controller('ruleFormCtrl', function ruleFormCtrl($scope) {
    var ts = $scope.ts = CRM.ts(null);
  });

})(angular, CRM.$, CRM._);
